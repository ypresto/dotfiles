[user]
	name = Yuya Tanaka
	email = yuya.presto@gmail.com
[core]
	excludesfile = ~/.gitignore_global
	editor = vim
[merge]
	tool = vimdiff
	renamelimit = 3000
[color]
	ui = auto
[alias]
	co = checkout
	ci = commit
	st = status
	br = branch
	hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
	g = log --graph --decorate --stat
	go = log --graph --decorate --stat --format=oneline
	gog = log --graph --decorate --format=oneline
	rs = reset
	rsh = reset --hard
	dw = diff --color-words
	ds = diff --stat
	cw = diff --color-words
	cs = diff --stat --cached
	lo = log --stat
	dt = difftool
	pr = pull --rebase
	ms = merge --squash
	re = rebase
[diff]
	tool = vimdiff
[difftool]
	prompt = true
[github]
	user = ypresto
[push]
	default = tracking
[rerere]
  enabled = true
